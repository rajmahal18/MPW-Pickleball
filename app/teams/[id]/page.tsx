import { notFound } from "next/navigation";import { prisma } from "@/lib/prisma";
export default async function TeamPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const team=await prisma.team.findUnique({where:{id},include:{group:true,pairs:{include:{playerA:true,playerB:true}},users:true}});if(!team)notFound();return <main className="mx-auto max-w-5xl px-4 py-8"><div className="label">{team.group.name}</div><h1 className="text-4xl font-black uppercase">{team.name}</h1><p className="mt-2 text-gray-500">Official 7-pair roster. Every player may appear only once within a team matchup.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{team.pairs.map((p,i)=><div className="panel p-4" key={p.id}><div className="label">Pair {i+1}</div><div className="mt-2 text-lg font-black">{p.playerA.firstName} {p.playerA.lastName}</div><div className="text-lg font-black">{p.playerB.firstName} {p.playerB.lastName}</div></div>)}</div></main>}
