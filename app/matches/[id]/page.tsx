import { notFound } from "next/navigation";import { prisma } from "@/lib/prisma";import ScoreBadge from "@/components/ScoreBadge";
export const dynamic="force-dynamic";
export default async function MatchPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const m=await prisma.matchup.findUnique({where:{id},include:{homeTeam:true,awayTeam:true,games:{include:{homePair:{include:{playerA:true,playerB:true}},awayPair:{include:{playerA:true,playerB:true}}},orderBy:{gameNumber:"asc"}}}});if(!m)notFound();return <main className="mx-auto max-w-6xl px-4 py-8"><div className="label">{m.groupLabel||m.stage} · {m.roundLabel}</div><div className="mt-2 grid items-center gap-4 bg-ink p-6 text-white md:grid-cols-[1fr_auto_1fr]"><h1 className="text-3xl font-black uppercase">{m.homeTeam?.name||"TBD"}</h1><ScoreBadge home={m.homeWins} away={m.awayWins} status={m.status}/><h1 className="text-3xl font-black uppercase md:text-right">{m.awayTeam?.name||"TBD"}</h1></div><div className="mt-6 space-y-3">{m.games.length?m.games.map(g=><div className="panel grid gap-4 p-4 md:grid-cols-[70px_1fr_auto_1fr] md:items-center" key={g.id}><span className="label">Game {g.gameNumber}</span><div className="font-bold">{g.homePair.playerA.firstName} / {g.homePair.playerB.firstName}</div><ScoreBadge home={g.homeScore} away={g.awayScore} status={g.status}/><div className="font-bold md:text-right">{g.awayPair.playerA.firstName} / {g.awayPair.playerB.firstName}</div></div>):<div className="panel p-8 text-center text-gray-500">Games appear after both team leaders submit lineups.</div>}</div></main>}
