import Link from "next/link";import { redirect } from "next/navigation";import { getCurrentUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function Admin(){const u=await getCurrentUser();if(!u||u.role!=="ADMIN")redirect("/login");const ms=await prisma.matchup.findMany({include:{homeTeam:true,awayTeam:true,games:true},orderBy:[{stage:"asc"},{order:"asc"}]});return <main className="mx-auto max-w-7xl px-4 py-8"><div className="flex items-start justify-between"><div><div className="label">Tournament control</div><h1 className="text-4xl font-black uppercase">Admin dashboard</h1></div><form action="/api/auth/logout" method="post"><button className="btn-ghost">Logout</button></form></div><form action="/api/admin/generate-knockout" method="post" className="mt-6"><button className="btn-primary">Generate / refresh knockout bracket</button></form><div className="panel mt-6 overflow-x-auto"><table className="w-full text-sm"><thead className="bg-ink text-left text-white"><tr><th className="p-3">Matchup</th><th className="p-3">Status</th><th className="p-3">Series</th><th className="p-3">Games</th></tr></thead><tbody>{ms.map(m=><tr className="border-b border-line" key={m.id}><td className="p-3 font-bold">{m.homeTeam?.shortName||"TBD"} vs {m.awayTeam?.shortName||"TBD"}<div className="text-xs font-normal text-gray-500">{m.groupLabel||m.stage} · {m.roundLabel}</div></td><td className="p-3">{m.status}</td><td className="p-3 font-black">{m.homeWins}-{m.awayWins}</td><td className="p-3"><div className="flex flex-wrap gap-2">{m.games.map(g=><Link className="btn-ghost px-2 py-1 text-xs" href={`/admin/score/${g.id}`} key={g.id}>G{g.gameNumber}</Link>)}</div></td></tr>)}</tbody></table></div></main>}
