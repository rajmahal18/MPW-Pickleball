import { redirect } from "next/navigation";import { getCurrentUser } from "@/lib/auth";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await getCurrentUser())redirect("/");const q=await searchParams;return <main className="mx-auto max-w-md px-4 py-16"><div className="panel p-6"><div className="label">Restricted access</div><h1 className="text-3xl font-black uppercase">Admin / Team leader</h1>{q.error&&<p className="mt-4 bg-red-50 p-3 text-sm text-red-700">Invalid email or password.</p>}<form className="mt-6 space-y-4" action="/api/auth/login" method="post"><label className="block text-sm font-bold">Email<input name="email" type="email" required className="mt-1 w-full border border-line p-3 outline-none focus:border-court"/></label><label className="block text-sm font-bold">Password<input name="password" type="password" required className="mt-1 w-full border border-line p-3 outline-none focus:border-court"/></label><button className="btn-primary w-full">Sign in</button></form><div className="mt-5 text-xs text-gray-500">Seed accounts: admin@rverse.test / admin123, leader1@rverse.test / leader123</div></div></main>}
